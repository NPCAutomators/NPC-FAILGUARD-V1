name: Windows install test

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  windows-install:
    runs-on: windows-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Run installer (skip Claude Code in CI)
        # PSModulePath is cleared because the runner's pwsh pollutes it for
        # Windows PowerShell 5.1; echo. feeds the "Press Enter" prompt.
        run: cmd /c "set PSModulePath=&& echo. | powershell -NoProfile -ExecutionPolicy Bypass -File install.ps1 -NoClaude"

      - name: Re-run installer (upgrade over existing install must not crash)
        run: |
          cmd /c "set PSModulePath=&& echo. | powershell -NoProfile -ExecutionPolicy Bypass -File install.ps1 -NoClaude"
          if ($LASTEXITCODE -ne 0) { throw "reinstall failed" }
          $run = Get-ItemProperty "HKCU:\Software\Microsoft\Windows\CurrentVersion\Run" -Name "NPC FailGuard" -ErrorAction SilentlyContinue
          if (-not $run) { throw "autostart Run-key entry missing" }
          Write-Host "autostart entry: $($run.'NPC FailGuard')"
          # the daemon the installer just started gets killed between steps by the runner - expected

      - name: Claude settings merge (claude-merge.py)
        run: |
          # space in the dir name mimics usernames like "Insha - allah"
          $Spaced = Join-Path $env:TEMP "npc ci - test"
          New-Item -ItemType Directory -Force $Spaced | Out-Null
          Copy-Item scripts\statusline.ps1 (Join-Path $Spaced "statusline.ps1")
          & core\.venv\Scripts\python.exe scripts\claude-merge.py `
            --settings (Join-Path $Spaced "ci-settings.json") `
            --claude-json (Join-Path $Spaced "ci-claude.json") `
            --statusline-ps1 (Join-Path $Spaced "statusline.ps1") `
            --plugin-dir "$PWD"
          Get-Content (Join-Path $Spaced "ci-settings.json")
          $cfg = Get-Content (Join-Path $Spaced "ci-settings.json") -Raw | ConvertFrom-Json
          if ($cfg.env.ANTHROPIC_BASE_URL -ne "http://127.0.0.1:8787") { throw "proxy env missing" }
          $cj = Get-Content (Join-Path $Spaced "ci-claude.json") -Raw | ConvertFrom-Json
          if (-not $cj.hasCompletedOnboarding) { throw "onboarding bypass missing" }

      - name: Run test suite on Windows
        run: |
          $py = Join-Path $PWD "core\.venv\Scripts\python.exe"
          uv pip install --quiet -r core\requirements-dev.txt --python $py
          & $py -m pytest tests/ -q

      - name: FULL matrix - every command x every condition (ends with uninstall)
        # One step: the runner kills background processes between steps, so the
        # matrix starts the daemon itself (service.ps1: hidden wscript path with
        # automatic direct-start fallback for CI) and tears down in Phase H.
        run: |
          & pwsh -NoProfile -File tests\e2e\matrix.ps1
          if ($LASTEXITCODE -ne 0) { throw "matrix failed ($LASTEXITCODE checks)" }

      - name: Uninstall again (must be idempotent)
        run: cmd /c "set PSModulePath=&& echo. | powershell -NoProfile -ExecutionPolicy Bypass -File uninstall.ps1 -Yes"

      - name: Diagnostics on failure
        if: failure()
        run: |
          Write-Host "--- proxy.log ---"
          Get-Content core\logs\proxy.log -Tail 80 -ErrorAction SilentlyContinue
          Write-Host "--- daemon.out / daemon.err (service.ps1 start) ---"
          Get-Content core\logs\daemon.out -Tail 40 -ErrorAction SilentlyContinue
          Get-Content core\logs\daemon.err -Tail 40 -ErrorAction SilentlyContinue
          Write-Host "--- daemon stderr/stdout (port-busy phase) ---"
          Get-Content "$env:TEMP\fg-daemon.err" -ErrorAction SilentlyContinue
          Get-Content "$env:TEMP\fg-daemon.out" -ErrorAction SilentlyContinue
          Write-Host "--- mock provider ---"
          Get-CimInstance Win32_Process -Filter "Name like 'python%'" -ErrorAction SilentlyContinue |
            Select-Object ProcessId, CommandLine | Format-List
